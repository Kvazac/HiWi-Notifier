api:
  base_url: "https://living.tum.de"
  result_limit: 50
  timeout_seconds: 30

matching:
  max_total_rent: 1200
  min_square_meters: null

  cities:
    - "München"
    - "Munich"

  types: []

  require_student_eligible: false

notifications:
  username: "TUM Living Watcher"

state:
  path: "data/living-state.json"
  first_run_mode: "mark_seen"
  retention_days: 180
